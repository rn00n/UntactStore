# active profiles
#spring.profiles.active=local

# 개발할 때에만 create-drop 또는 update를 사용하고 운영 환경에서는 validate를 사용.
#spring.jpa.hibernate.ddl-auto=create-drop

# SQL 로깅을 하여 어떤 값으로 어떤 SQL이 실행되는지 확인합니다.
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# 톰캣 기본 요청 사이즈 2MB.
server.tomcat.max-http-form-post-size=5MB

# 웹 서버 호스트 (개발)
app.host=http://localhost:8080

# HTML <FORM>에서 th:method에서 PUT 또는 DELETE를 사용해서 보내는 _method를 사용해서  @PutMapping과 @DeleteMapping으로 요청을 맵핑.
spring.mvc.hiddenmethod.filter.enabled=true


############
spring.jpa.hibernate.ddl-auto=update

server.port=8081
spring.datasource.url=jdbc:postgresql://localhost:5432/untacstore
spring.datasource.username=readadream
spring.datasource.password=1234

# Java Email Sender
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=rn00n.dev@gmail.com
spring.mail.password=huzpswnfcuinewbd
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.starttls.enable=true